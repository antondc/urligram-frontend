image: node:20

before_script:
  - apt-get update -qq
  - apt-get install -qq git openssh-client

  # Create SSH directory
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh

  # Start ssh-agent ONCE
  - eval $(ssh-agent -s)

  # Add SERVER_KEY (base64)
  - echo "$SERVER_KEY" | base64 -d > ~/.ssh/id_rsa_server
  - chmod 600 ~/.ssh/id_rsa_server
  - ssh-add ~/.ssh/id_rsa_server

  # Add GitHub deploy key (base64)
  - echo "$GITHUB_KEY" | base64 -d > ~/.ssh/id_rsa_github
  - chmod 600 ~/.ssh/id_rsa_github
  - ssh-add ~/.ssh/id_rsa_github

  # Disable strict host key checking
  - echo -e "Host *\n\tStrictHostKeyChecking no\n" > ~/.ssh/config

stages:
  - github_clone
  - install
  - test
  - build_production
  - deploy_production

cache:
  paths:
    - node_modules/

github_clone:
  stage: github_clone
  variables:
    GIT_DEPTH: "0"
  script:
    - git checkout -B "$CI_COMMIT_REF_NAME"
    - git remote remove github || true
    - git remote add github "git@github.com:${GITHUB_ACCOUNT}/${GITHUB_PROJECT}.git"
    - git push github --all
    - git push github --tags

install:
  stage: install
  script:
    - npm install --verbose -f
  artifacts:
    paths:
      - node_modules/

test:
  stage: test
  script:
    - npm test
  artifacts:
    paths:
      - node_modules/

build_prod:
  stage: build_production
  only:
    - master
  script:
    - npm run prod

deploy_production:
  stage: deploy_production
  only:
    - master
  script:
    - ssh ${SERVER_USER}@${SERVER_ADDRESS} "
        cd ${SERVER_PATH_PRODUCTION}
        && rm -rf node_modules dist/* tmp
      "
    - ssh ${SERVER_USER}@${SERVER_ADDRESS} "
        cd ${SERVER_PATH_PRODUCTION}
        && git pull origin master
      "
    - ssh ${SERVER_USER}@${SERVER_ADDRESS} "
        source ~/.bash_profile
        && export NPM_TOKEN=${NPM_TOKEN}
        && export JWT_SECRET=superSecret
        && cd ${SERVER_PATH_PRODUCTION}
        && nvm use --delete-prefix
        && git checkout master
        && npm ci --verbose
        && npm run prod
      "
    - ssh ${SERVER_USER}@${SERVER_ADDRESS} "
        source ~/.bash_profile
        && export NPM_TOKEN=${NPM_TOKEN}
        && cd ${SERVER_PATH_PRODUCTION}
        && nvm use --delete-prefix
        && pm2 delete api.urligram.com || true
        && export JWT_SECRET=${JWT_SECRET}
        && NODE_ENV=production pm2 start ./dist/server.js --name urligram.com --update-env
      "
